---
phase: 01-api
plan: "01-02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/src/insight/
  - backend/src/llm/
  - backend/package.json
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - API-02
user_setup:
  - Set AI_BASE_URL, AI_API_KEY, AI_MODEL in backend/.env for live LLM calls.

must_haves:
  truths:
    - "POST /v1/insight/questions calls upstream chat/completions (or compatible) once per request and returns parsed JSON with 3 questions"
    - "POST /v1/insight/submit generates conclusion text via LLM using user answers + questions snapshot"
    - "Malformed LLM JSON triggers controlled error (502/500 with code) without inventing silent wrong shapes"
  artifacts:
    - "backend/src/llm/llm.service.ts (or equivalent) implements single HTTP path"
    - "Prompt templates live as constants or template strings versioned in code"
  key_links:
    - "InsightService delegates to LlmService; no duplicate fetch implementations"
---

<objective>
Replace stub insight generation with a **single OpenAI-compatible** LLM integration: prompt for three structured questions + JSON parsing, and prompt for tendency conclusion text aligned with AI-01–AI-03 and `docs/tasks/002-ai-insight-question-generation.md`.
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Prompt injection via rawQuestion | System prompt instructs model to output JSON only; strip/control length on input (max chars constant e.g. 2000) |
| API key leakage | Keys only from env; never log full key; errors omit upstream body secrets |
| Supplier outage | Map upstream errors to 502 + `{ code, message, requestId }` |
</threat_model>

<execution_context>
@.planning/phases/01-api/01-CONTEXT.md
@docs/tasks/002-ai-insight-question-generation.md
@demo/web/src/app/api/ai/route.ts
</execution_context>

<tasks>

<task type="auto">
  <name>T1 — LlmService: OpenAI-compatible POST</name>
  <files>backend/src/llm/llm.module.ts, backend/src/llm/llm.service.ts</files>
  <read_first>demo/web/src/app/api/ai/route.ts, backend/src/app.module.ts, backend/.env.example</read_first>
  <action>Create `LlmService` with method `completeChat({ system, user }: { system: string; user: string }): Promise&lt;{ text: string; latencyMs: number }&gt;` using native `fetch` to `${AI_BASE_URL}/v1/chat/completions` (document exact path in code comment if provider differs — adjust via env base URL). Headers: `Authorization: Bearer ${AI_API_KEY}`, `Content-Type: application/json`. Body: `{ model: process.env.AI_MODEL, messages: [...], temperature }`. Parse assistant message content from response JSON path `choices[0].message.content`. Throw typed errors on non-2xx. Export `LlmModule` as `@Global()` or import into `InsightModule`.</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `backend/src/llm/llm.service.ts` exists
    - File contains string `chat/completions` or configurable path documented
    - `grep -r LlmModule backend/src/` shows registration
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T2 — Questions prompt + JSON schema validation</name>
  <files>backend/src/insight/prompts/questions.prompt.ts, backend/src/insight/insight.service.ts</files>
  <read_first>docs/tasks/002-ai-insight-question-generation.md, backend/src/insight/dto/questions-response.dto.ts (create if needed)</read_first>
  <action>Implement system+user prompts requiring exactly 3 questions with dimensions `inner_preference`, `fear_boundary`, `active_vs_avoidance` in order, each with 2–4 options, IDs stable (`q1`,`q2`,`q3` or generated). After LLM returns text, parse JSON (strip markdown fences if present). Validate with runtime schema (zod recommended — add dependency `zod` if not present). On validation failure throw to exception layer → 502 `LLM_OUTPUT_INVALID`. Replace stub in `InsightService.generateQuestions`.</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `backend/package.json` dependencies include zod if used
    - Insight service uses `LlmService.completeChat` for questions path
    - Code references three dimension strings matching 002 doc
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T3 — Conclusion prompt</name>
  <files>backend/src/insight/prompts/conclusion.prompt.ts, backend/src/insight/insight.service.ts</files>
  <read_first>docs/product-scope.md, docs/tasks/002-ai-insight-question-generation.md</read_first>
  <action>Build user payload including `rawQuestion`, serialized questions+selected labels, system prompt forbidding medical diagnosis and imperative commands; ask for single paragraph tendency insight in Chinese (product language). Call `LlmService.completeChat`. Return `{ conclusion: text }`. Replace stub in submit path.</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `conclusion.prompt.ts` or equivalent contains explicit non-medical constraint keywords (e.g. 非医疗 / 不代替专业诊断 or English equivalent per docs)
    - Submit handler returns LLM text trimmed non-empty on happy path
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] With valid env, integration smoke: one curl questions + one curl submit returns non-stub coherent text (document in plan SUMMARY later)
- [ ] `cd backend && npm run lint` passes
</verification>

---

## PLANNING COMPLETE
