---
phase: 02-android-ui
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - clients/flutter/android/app/src/main/AndroidManifest.xml
  - clients/flutter/android/app/src/main/res/xml/network_security_config.xml
  - clients/flutter/README.md
  - clients/android/README.md
  - docs/tech-stack.md
  - docs/architecture.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05]
must_haves:
  truths:
    - "Android delivery path is documented as Flutter-first in clients/flutter"
    - "clients/android is documented as an entry/readme handoff, not the Phase 2 implementation home"
    - "Flutter Android dev config supports local backend integration without weakening production HTTPS expectations"
    - "Docs no longer contradict D-01 through D-05"
  artifacts:
    - path: "clients/flutter/android/app/src/main/AndroidManifest.xml"
      provides: "Android app network configuration hook"
    - path: "clients/flutter/android/app/src/main/res/xml/network_security_config.xml"
      provides: "Scoped local cleartext allowance if needed for emulator development"
    - path: "clients/flutter/README.md"
      provides: "Flutter Phase 2 run/config instructions"
    - path: "clients/android/README.md"
      provides: "Android directory role points to Flutter implementation"
    - path: "docs/tech-stack.md"
      provides: "Flutter-first Phase 2 client stack decision"
    - path: "docs/architecture.md"
      provides: "Architecture directory mapping aligned to Flutter-first client"
    - path: ".planning/PROJECT.md"
      provides: "Planning source of truth aligned to Flutter-first Phase 2 Android delivery"
  key_links:
    - from: "docs/tech-stack.md"
      to: "clients/flutter/README.md"
      via: "Phase 2 Flutter-first decision"
      pattern: "Flutter"
    - from: "clients/flutter/README.md"
      to: "backend/README.md"
      via: "local backend API/header setup"
      pattern: "API_TOKEN|X-Device-Id|Authorization"
    - from: ".planning/PROJECT.md"
      to: "clients/flutter"
      via: "Active client and key decision guidance"
      pattern: "clients/flutter|Flutter"
---

<objective>
Finalize Android local integration configuration and synchronize stale repository docs with the locked Flutter-first Phase 2 decision.

Purpose: D-05 is a hard requirement: implementation and team-facing docs must stop saying Android native/WebView first or Flutter later. Android development must also make local base URL and cleartext behavior explicit without putting AI/vendor secrets in the client.
Output: Android network config and docs aligned with the completed Flutter MVP flow.
</objective>

<execution_context>
@/Users/80319084/Work/Codes/FavorMe/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/80319084/Work/Codes/FavorMe/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-android-ui/02-CONTEXT.md
@.planning/phases/02-android-ui/02-UI-SPEC.md
@.planning/phases/02-android-ui/02-PATTERNS.md
@docs/tech-stack.md
@docs/architecture.md
@clients/android/README.md
@clients/flutter/README.md
@docs/deployment.md
@backend/README.md
</context>

## Phase Goal

**As a** developer preparing the Android MVP demo, **I want to** run the Flutter client against the Phase 1 backend with clear docs, **so that** the app can be handed off without contradictory Android native/WebView guidance.

<tasks>

<task type="auto">
  <name>Task 1: Configure Android dev network behavior and Flutter run documentation</name>
  <read_first>
    - `clients/flutter/README.md`
    - `backend/README.md`
    - `docs/deployment.md`
    - `clients/flutter/android/app/src/main/AndroidManifest.xml`
  </read_first>
  <files>clients/flutter/android/app/src/main/AndroidManifest.xml, clients/flutter/android/app/src/main/res/xml/network_security_config.xml, clients/flutter/README.md</files>
  <action>Update Flutter Android configuration and README for local backend integration. In `AndroidManifest.xml`, keep only required Internet/network configuration and reference `@xml/network_security_config` if local HTTP is needed for emulator development. Create `network_security_config.xml` that permits cleartext only for local development hosts such as `10.0.2.2`, `localhost`, or `127.0.0.1`; do not make production domains cleartext. In `clients/flutter/README.md`, document Phase 2 as the main Android implementation, local run commands, backend base URL guidance (`10.0.2.2:3000` for Android emulator and LAN host for physical device), `--dart-define` or equivalent local config for `API_BASE_URL` and `API_TOKEN`, and the required headers `Authorization: Bearer <token>` plus non-empty `X-Device-Id`. State that AI/vendor keys and prompts remain backend-only.</action>
  <verify>
    <automated>cd clients/flutter && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `clients/flutter/android/app/src/main/AndroidManifest.xml` contains `android.permission.INTERNET`.
    - If `usesCleartextTraffic` appears, `clients/flutter/android/app/src/main/res/xml/network_security_config.xml` also exists and contains only local development hosts, not wildcard production domains.
    - `clients/flutter/README.md` contains `Flutter-first`, `API_BASE_URL`, `API_TOKEN`, `Authorization: Bearer`, `X-Device-Id`, `10.0.2.2`, and `backend-only`.
    - `clients/flutter/README.md` does not say `MVP/首版 不依赖本目录`, `WebView 薄壳`, or `MVP 之后 在此目录初始化`.
  </acceptance_criteria>
  <done>Android local API access is explicit, production HTTPS remains the stated expectation, and Flutter README gives the executor/user enough information to run the Phase 2 app.</done>
</task>

<task type="auto">
  <name>Task 2: Sync stale client architecture, stack, and project docs</name>
  <read_first>
    - `.planning/phases/02-android-ui/02-CONTEXT.md`
    - `.planning/PROJECT.md`
    - `docs/tech-stack.md`
    - `docs/architecture.md`
    - `clients/android/README.md`
    - `clients/flutter/README.md`
  </read_first>
  <files>.planning/PROJECT.md, docs/tech-stack.md, docs/architecture.md, clients/android/README.md, clients/flutter/README.md</files>
  <action>Update stale docs per D-01 through D-05. In `.planning/PROJECT.md`, update the "What This Is", active client requirement, context, constraints, and key decisions sections so the Phase 2 Android delivery path is Flutter-first in `clients/flutter`, while `clients/android` is only an explanatory/platform entry and does not carry MVP feature implementation. In `docs/tech-stack.md`, replace the Phase 2 client direction that says Android native first / `clients/android` first / Flutter later with Flutter-first in `clients/flutter`, Android-only delivery for this phase, and iOS-reusable structure without iOS verification. In `docs/architecture.md`, update the clients directory mapping so `clients/flutter` is the current Phase 2 Android MVP implementation and `clients/android` is an explanatory/platform entry, not the main implementation. In `clients/android/README.md`, replace the WebView thin-shell guidance with a short handoff pointing to `../flutter` for Phase 2 Android implementation and documenting that this directory does not carry the MVP feature code. Keep the docs clear that full chat history, voice input, formal auth, payments, and iOS release remain out of scope for this phase.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
checks = {
  '.planning/PROJECT.md': ['Flutter', 'clients/flutter', 'Phase 2', 'Android'],
  'docs/tech-stack.md': ['Flutter', 'clients/flutter', 'Android-only'],
  'docs/architecture.md': ['clients/flutter', 'Phase 2', 'Android MVP'],
  'clients/android/README.md': ['clients/flutter', '不承载', 'Phase 2'],
  'clients/flutter/README.md': ['Flutter-first', 'Android-only', 'X-Device-Id'],
}
for path, needles in checks.items():
    text = Path(path).read_text()
    missing = [needle for needle in needles if needle not in text]
    if missing:
        raise SystemExit(f'{path} missing {missing}')
for path in ['.planning/PROJECT.md', 'docs/tech-stack.md', 'docs/architecture.md', 'clients/android/README.md', 'clients/flutter/README.md']:
    text = Path(path).read_text()
    forbidden = ['MVP/首版 不依赖本目录', 'WebView 薄壳 + `demo/web` H5', 'Android native', 'Android 原生', 'Flutter | 继续保留长期双端收敛选项']
    found = [token for token in forbidden if token in text]
    if found:
        raise SystemExit(f'{path} still has stale text {found}')
print('doc sync ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` describes the active Phase 2 Android client as Flutter-first in `clients/flutter` across "What This Is", active client requirement, context, constraints, and key decisions.
    - `docs/tech-stack.md` contains `clients/flutter` and `Android-only`.
    - `docs/architecture.md` maps `clients/flutter` to the current Phase 2 Android MVP implementation.
    - `clients/android/README.md` points to `../flutter` and says it does not carry Phase 2 feature implementation.
    - The Python verification command prints `doc sync ok`.
  </acceptance_criteria>
  <done>Planning and repository docs agree with the locked Flutter-first implementation decision and no longer direct Phase 2 work to Android native/WebView/Flutter-later paths.</done>
</task>

<task type="auto">
  <name>Task 3: Run final source gates for client security and phase readiness</name>
  <read_first>
    - `clients/flutter/lib/src/features/insight/insight_api_client.dart`
    - `clients/flutter/lib/src/features/insight/device_id_store.dart`
    - `clients/flutter/README.md`
    - `docs/tasks/003-phase1-api-auth-headers.md`
  </read_first>
  <files>clients/flutter/README.md</files>
  <action>Add a short `Security and local configuration` section to `clients/flutter/README.md` if it does not already exist. It must state: use backend `API_TOKEN` only for development, never commit real production tokens, never put AI/vendor keys or prompt templates in Flutter, use HTTPS outside local development, device id is a stable development identifier and not formal auth, and raw question bodies should not be logged by the client. Then run source gates across `clients/flutter` to enforce those constraints.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
root = Path('clients/flutter')
text = '\n'.join(p.read_text(errors='ignore') for p in root.rglob('*') if p.is_file() and p.suffix in {'.dart', '.yaml', '.xml', '.md'})
required = ['Authorization', 'X-Device-Id', 'API_TOKEN']
for needle in required:
    if needle not in text:
        raise SystemExit(f'missing {needle}')
forbidden = ['AI_API_KEY', 'ARK_API_KEY', 'OPENAI_API_KEY', 'chat/completions', 'Riverpod', 'flutter_bloc']
found = [needle for needle in forbidden if needle in text]
if found:
    raise SystemExit(f'forbidden client text found: {found}')
readme = Path('clients/flutter/README.md').read_text()
for needle in ['backend-only', 'HTTPS', 'raw question', 'do not log']:
    if needle not in readme:
        raise SystemExit(f'README missing security note: {needle}')
print('client security gates ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `clients/flutter/README.md` contains `backend-only`, `HTTPS`, `raw question`, and `do not log`.
    - The Python source gate prints `client security gates ok`.
    - No client source contains `AI_API_KEY`, `ARK_API_KEY`, `OPENAI_API_KEY`, or `chat/completions`.
  </acceptance_criteria>
  <done>Phase 2 client docs and source gates explicitly address mobile token handling, backend-only AI secrets, device id persistence, local cleartext/base URL behavior, and raw-question logging boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local developer config to Android app | Base URL and development token enter the mobile process |
| Android app to local backend over HTTP | Emulator/device may use local cleartext during development |
| Repository docs to future implementation | Stale guidance can route work to the wrong client or unsafe secret handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | `clients/flutter/README.md` | mitigate | Document that API tokens are development config, production secrets are not committed, and AI/vendor keys remain backend-only. |
| T-02-04-02 | Information Disclosure | `network_security_config.xml` | mitigate | Scope cleartext to local development hosts only; docs keep production HTTPS-only expectation. |
| T-02-04-03 | Spoofing | `device_id_store.dart` docs | mitigate | State device id is a stable MVP identifier, not formal auth; formal auth remains out of scope. |
| T-02-04-04 | Repudiation | client logging guidance | mitigate | Document that raw question bodies should not be logged by the client beyond backend contract needs. |
| T-02-04-05 | Tampering | stale docs | mitigate | Replace Android native/WebView/Flutter-later guidance so executors and users do not build the wrong surface. |
</threat_model>

<verification>
Run `cd clients/flutter && flutter analyze`, then run both Python verification snippets from the tasks. As a final manual-free gate, `rg "WebView 薄壳|Android 原生 App 优先|Android native|Android 原生|Flutter \\| 继续保留长期双端收敛选项|MVP/首版 不依赖本目录" .planning/PROJECT.md docs/tech-stack.md docs/architecture.md clients/android/README.md clients/flutter/README.md` should return no stale Phase 2 guidance.
</verification>

<success_criteria>
- D-05 is complete because `.planning/PROJECT.md`, `docs/tech-stack.md`, `docs/architecture.md`, `clients/android/README.md`, and `clients/flutter/README.md` are synced with Flutter-first Phase 2.
- Android local backend integration is documented without making production cleartext acceptable.
- Security requirements are explicitly covered: mobile token handling, backend-only AI/vendor secrets, device id persistence, local base URL/cleartext behavior, and avoiding raw-question client logs.
- The completed Phase 2 flow remains Android-only in delivery while keeping Flutter code reusable for later iOS work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-android-ui/02-04-SUMMARY.md`.
</output>
