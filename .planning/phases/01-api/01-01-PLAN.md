---
phase: 01-api
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/
  - backend/src/app.module.ts
  - backend/src/main.ts
  - backend/.env.example
  - backend/README.md
  - docs/tasks/003-phase1-api-auth-headers.md
autonomous: true
requirements:
  - API-01
  - API-02
user_setup:
  - Create PostgreSQL database and set DATABASE_URL before running migrations.

must_haves:
  truths:
    - "curl can call POST /v1/users/bootstrap with Authorization Bearer and X-Device-Id and receive JSON with userId"
    - "curl can call POST /v1/insight/questions with auth headers and receive exactly 3 questions with dimension field per docs/tasks/002 (stub content allowed)"
    - "curl can call POST /v1/insight/submit with raw_question, questions snapshot, and answers and receive a conclusion string (stub allowed)"
  artifacts:
    - "backend/src/**/*insight*.ts (module/controller/service) exists"
    - "backend/prisma/migrations contains new migration for User.deviceId"
  key_links:
    - "AppModule imports new InsightModule and user bootstrap module"
    - "GlobalValidationPipe unchanged; route handlers use DTOs with class-validator"
---

<objective>
Establish Nest routes, guards, DTOs, and Prisma field for device-scoped users so the insight flow is callable end-to-end with **stubbed** LLM output (valid JSON shapes). Deliver Bearer + mandatory `X-Device-Id`, user bootstrap, and consistent 4xx JSON error body shape including `requestId`.
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Bearer token brute-force | Dev-only token in env; document rotation; no default token in repo |
| Missing device header bypass | Global guard returns 400 without processing body |
| PII in logs | Do not log raw_question body in Phase 1 stubs; only length/hash in later plan |
</threat_model>

<execution_context>
@.planning/phases/01-api/01-CONTEXT.md
@docs/tasks/002-ai-insight-question-generation.md
</execution_context>

<tasks>

<task type="auto">
  <name>T1 — Prisma: User.deviceId + migration</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma, docs/tasks/002-ai-insight-question-generation.md</read_first>
  <action>Add optional unique field `deviceId String? @unique` on model `User` (nullable for legacy rows). Run `npx prisma migrate dev --name add_user_device_id` from `backend/` (or create equivalent SQL migration folder) so schema and migration SQL are committed. Regenerate client.</action>
  <verify>cd backend && npx prisma validate</verify>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains `deviceId` on `User`
    - `backend/prisma/migrations/` contains a new migration directory after migrate
    - `grep -q deviceId backend/prisma/schema.prisma`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T2 — Config: DEV_API_TOKEN (or API_TOKEN) + document</name>
  <files>backend/.env.example, backend/README.md, docs/tasks/003-phase1-api-auth-headers.md</files>
  <read_first>backend/src/app.module.ts, backend/.env.example, .planning/phases/01-api/01-CONTEXT.md</read_first>
  <action>Add env var `API_TOKEN` (single name — use consistently) for Bearer validation; add `AI_BASE_URL`, `AI_API_KEY`, `AI_MODEL` placeholders to `.env.example` for Plan 01-02. Create **docs/tasks/003-phase1-api-auth-headers.md** describing: required headers `Authorization: Bearer &lt;API_TOKEN&gt;`, required `X-Device-Id`, optional/generated `X-Request-Id`; 400 if device missing; 401 if bearer wrong. README section mirrors this with curl examples.</action>
  <verify>grep -q API_TOKEN backend/.env.example</verify>
  <acceptance_criteria>
    - `backend/.env.example` contains API_TOKEN= and AI_BASE_URL=
    - `docs/tasks/003-phase1-api-auth-headers.md` exists and mentions Bearer and X-Device-Id
    - `backend/README.md` links to 003 doc or duplicates the table in short form
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T3 — Guards: Bearer + DeviceId + RequestId</name>
  <files>backend/src/common/guards/api-token.guard.ts, backend/src/common/middleware/request-id.middleware.ts (or interceptor)</files>
  <read_first>backend/src/main.ts, backend/src/app.module.ts</read_first>
  <action>Implement `ApiTokenGuard` comparing `Authorization` header `Bearer` token to `process.env.API_TOKEN`. Implement guard or pipe that rejects with 400 if `X-Device-Id` is absent or empty string. Add middleware/interceptor: if `X-Request-Id` absent, set `request.requestId = randomUUID()`; attach same id to response header `X-Request-Id`. Register globally or on insight + user routes.</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - Guard file exists under `backend/src/` and is referenced from module(s)
    - `backend/src/main.ts` or `app.module.ts` wires request id behavior
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T4 — POST /v1/users/bootstrap</name>
  <files>backend/src/users/users.module.ts, backend/src/users/users.controller.ts, backend/src/users/users.service.ts</files>
  <read_first>backend/prisma/schema.prisma, backend/src/prisma/prisma.service.ts</read_first>
  <action>Upsert `User` by `deviceId = X-Device-Id` header value (create if missing). Return `{ "userId": "&lt;cuid&gt;" }`. Use Prisma `upsert` with `where: { deviceId }` — requires deviceId non-null on create; for lookup device header must be present (already enforced).</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `POST /v1/users/bootstrap` registered under global prefix `v1`
    - Response JSON includes key `userId`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T5 — Insight routes with stub services</name>
  <files>backend/src/insight/insight.module.ts, backend/src/insight/insight.controller.ts, backend/src/insight/insight.service.ts, backend/src/insight/dto/*.ts</files>
  <read_first>docs/tasks/002-ai-insight-question-generation.md, .planning/phases/01-api/01-CONTEXT.md</read_first>
  <action>Implement `POST /v1/insight/questions` body: `{ "rawQuestion": string, "inputMode"?: "text" | "voice_to_text" }`. Return JSON with exactly 3 items in `questions`, each with `id`, `dimension` in (`inner_preference`|`fear_boundary`|`active_vs_avoidance`), `title`, `options` array of `{ id, label }` (2–4 options). Stub fixed copy acceptable. Implement `POST /v1/insight/submit` body: `{ "rawQuestion": string, "questions": [...], "answers": [{ "questionId": string, "optionId": string }] }` returning `{ "conclusion": string }` stub text that avoids medical/command tone per product-scope. Wire modules into `AppModule`. Map validation failures to HTTP exception filter producing `{ code, message, requestId }`.</action>
  <verify>cd backend && npm run build && npm run lint</verify>
  <acceptance_criteria>
    - `insight.controller.ts` defines both POST routes
    - Stub response for questions has length 3 for `questions` array (grep or test script)
    - Error responses include `requestId` field in JSON body
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && npm run build` exits 0
- [ ] Manual curl sequence from README succeeds against running server (document commands in README)
</verification>

---

## PLANNING COMPLETE
