# Demo

产品演示与流程验证，**不替代**正式 `backend` 与移动客户端。

## `web/`

Next.js 应用，见 [`web/README.md`](web/README.md)。

```bash
cd web
npm install
npm run dev
```

## 注意

- 环境变量文件为 `web/.env.local`（见该目录 README）。
- 演示中登录、数据存储等多为**本地/固定验证码**，与商用后端行为不同。
