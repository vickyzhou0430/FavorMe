---
phase: 01-api
plan: "01-03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/
  - backend/src/insight/
  - backend/src/common/filters/
  - backend/src/main.ts
autonomous: true
requirements:
  - API-03
  - QAS-02
user_setup:
  - Run `npx prisma migrate deploy` (or dev migrate) after pulling schema changes for InsightRound.

must_haves:
  truths:
    - "Invalid/nonsense rawQuestion returns 422 with code aligned to INVALID_QUESTION_INPUT per 002 (no fabricated questions)"
    - "Logs include requestId for each insight request; searchable in stdout JSON lines or structured fields"
    - "Successful submit persists one record with userId, rawQuestion, questionsJson, answersJson, conclusion, requestId"
  artifacts:
    - "Prisma model for completed insight round exists + migration"
    - "HttpExceptionFilter or equivalent attaches requestId to error JSON"
  key_links:
    - "Insight submit writes DB after LLM success only (D-10)"
---

<objective>
Harden input validation (API-03), ensure **QAS-02** request correlation in logs and responses, and persist **one row per completed round** (D-10) without storing in-flight state between the two insight calls.
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Log injection | Log structured JSON; escape or truncate user strings |
| DB exhaustion | Optional payload size limits on JSON columns / request body parser limit |
| Replay of submit | Out of scope Phase 1 — note in README; idempotency deferred |
</threat_model>

<execution_context>
@.planning/phases/01-api/01-CONTEXT.md
@docs/tasks/002-ai-insight-question-generation.md
</execution_context>

<tasks>

<task type="auto">
  <name>T1 — Prisma: InsightRound (or equivalent) model</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma, .planning/phases/01-api/01-CONTEXT.md</read_first>
  <action>Add model `InsightRound` with fields: `id` cuid, `createdAt`, `userId` FK → User, `rawQuestion` String @db.Text, `questionsJson` Json, `answersJson` Json, `conclusion` String @db.Text, `requestId` String (index). Relation `user User @relation(...)`. Add `insightRounds InsightRound[]` on User. Create migration `add_insight_round`. **[BLOCKING]** After modifying schema files, run `cd backend && npx prisma migrate dev --name add_insight_round` (or generate SQL migration) before marking verification green.</action>
  <verify>cd backend && npx prisma validate && npx prisma generate</verify>
  <acceptance_criteria>
    - `InsightRound` exists in schema.prisma
    - New migration folder exists under `backend/prisma/migrations/`
    - `grep -q InsightRound backend/prisma/schema.prisma`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T2 — Invalid question gate (API-03)</name>
  <files>backend/src/insight/insight.service.ts, backend/src/insight/validators/question-input.validator.ts</files>
  <read_first>docs/tasks/002-ai-insight-question-generation.md</read_first>
  <action>Before calling LLM for questions: validate `rawQuestion` — trim, min length (e.g. ≥4 chars), max length (e.g. ≤2000), reject empty/gibberish patterns optionally via simple heuristic OR lightweight classifier stub (document MVP heuristic). On failure throw `UnprocessableEntityException` with body `{ code: 'INVALID_QUESTION_INPUT', message: localized string, requestId }`. Never call LLM when gate fails.</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - Handler maps 422 to stable `INVALID_QUESTION_INPUT` string in response JSON
    - README or DTO documents limits (min/max length)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T3 — Structured logging + persist on submit</name>
  <files>backend/src/insight/insight.service.ts, backend/src/main.ts</files>
  <read_first>backend/src/main.ts, backend/prisma/schema.prisma</read_first>
  <action>Use Nest `Logger` or `pino` (only if already justified — prefer Nest Logger with JSON-like context object) to log lines containing `requestId`, route name, userId, durationMs. On successful `POST /v1/insight/submit`, insert `InsightRound` with snapshot fields; associate `userId` from JWT-less flow: resolve user by `X-Device-Id` same as bootstrap (inject UsersService or duplicate minimal lookup). Do **not** persist on questions-only request (D-10).</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `InsightRound` create called only from submit success path (grep `insightRound.create` or `prisma.insightRound.create`)
    - Log statement includes variable holding request id (grep `requestId` in insight.service.ts)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>T4 — Exception filter: requestId on all errors</name>
  <files>backend/src/common/filters/http-exception.filter.ts, backend/src/main.ts</files>
  <read_first>backend/src/main.ts</read_first>
  <action>Register global exception filter that ensures JSON shape `{ statusCode, code?, message, requestId }` from request object set by middleware. Nest default filter may be extended — ensure 4xx/5xx from insight routes always echo `requestId`.</action>
  <verify>cd backend && npm run build</verify>
  <acceptance_criteria>
    - `main.ts` uses `app.useGlobalFilters` or equivalent with custom filter file
    - Filter source references `requestId`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `[BLOCKING]` `cd backend && npx prisma migrate deploy` succeeds against dev DB (or document dev-only migrate)
- [ ] `cd backend && npm run lint && npm run build`
- [ ] curl invalid body → 422 with requestId in JSON body
</verification>

---

## PLANNING COMPLETE
